<nav class="pagination" role="navigation">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}">&larr; Articlos mas recientes</a>
    {{/if}}
    <span class="page-number">Pagina {{page}} de {{pages}}</span>
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Articulos mas antiguos &rarr;</a>
    {{/if}}
</nav>